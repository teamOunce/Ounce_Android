<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.teamounce.ounce">
    <!-- 인터넷 사용 권한 설정 -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- 네트워크 연결 상태 확인 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".OunceApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OUNCE"
        android:usesCleartextTraffic="true"
        tools:replace="android:theme">
        <activity android:name=".settings.SettingsIntroActivity" />
        <activity android:name=".settings.SettingsOpensourceActivity" />
        <activity android:name=".settings.SettingsPolicyActivity" />
        <activity android:name=".settings.SettingsAgreeActivity" />
        <activity android:name=".feed.ui.FeedActivity"
            android:theme="@style/FeedTheme"
            android:exported="true"/>
        <activity
            android:name=".feed.ui.FoodDetailActivity"
            android:exported="true" />
        <activity
            android:name=".review.ui.RecordActivity"
            android:exported="true" />
        <activity android:name=".settings.SettingsCareActivity" />
        <activity android:name=".settings.SettingsActivity" />
        <activity
            android:name=".review.ui.SearchActivity"
            android:exported="true" />

        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="@string/native_app_key" />

        <activity android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="oauth"
                    android:scheme="kakao7171285de2b4a7896ab049321ffc8ed6" />
            </intent-filter>
        </activity>
        <activity
            android:name=".register.ui.SignUpActivity"
            android:exported="true" />
        <activity
            android:name=".login.ui.LoginActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main.MainActivity" />
    </application>

</manifest>